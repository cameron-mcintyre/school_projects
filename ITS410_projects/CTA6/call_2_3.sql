/*Code at least two CALL statements that test this procedure. (Note that this table doesn’t allow duplicate category names.) Execute the query and take a screenshot of the query and the results.*/

CALL insert_category('Pianos');

CALL insert_category('Kazoos');